/**
 * Compliance Gate badge — dignified ochre seal.
 * Re-exports the shared implementation so existing imports keep working.
 */
export { ComplianceGateBadge } from "@/components/admin/_shared/badges"
